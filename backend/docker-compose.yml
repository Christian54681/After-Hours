version: '3.8'

services:
  mongodb:
    image: mongo:7
    container_name: after_hours_db
    ports:
      - "27017:27017"
    environment:
      MONGO_INITDB_DATABASE: after_hours
    volumes:
      - mongo_data:/data/db

volumes:
  mongo_data: